# FoxSwift

This is the repository for the FoxSwift project.

## Getting Started

To set up your local development environment, run the setup script:

```sh
bash .specify/scripts/bash/setup.sh
```

This script will check for necessary dependencies (like `pre-commit` and `git-lfs`), install them if needed, and initialize the project hooks.
